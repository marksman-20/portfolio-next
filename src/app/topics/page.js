import TopicsClient from './TopicsClient';

export const metadata = {
  title: 'Topics — Manas Pokley',
  description: 'A library of climate, finance, agri-tech and Earth-observation notes.',
};

export default function TopicsPage() {
  return <TopicsClient />;
}
